import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModalcrearmoderadorPage } from './modalcrearmoderador.page';

describe('ModalcrearmoderadorPage', () => {
  let component: ModalcrearmoderadorPage;
  let fixture: ComponentFixture<ModalcrearmoderadorPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(ModalcrearmoderadorPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
